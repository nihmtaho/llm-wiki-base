# - Prefers Vietnamese for communication. Confidence: 0.8
- Prefers Vietnamese for communication. Confidence: 0.8
- Prefers naming components to align with the project name ("llm-wiki-base" prefix, e.g. default MCP server named "llm-wiki-base-mcp"). Confidence: 0.8
- Prefers a centralized registry pattern: a single MCP server entry that aggregates multiple wiki instances registered by name+path in a central config, rather than spawning one server per wiki. Confidence: 0.85
- Prefers TOML for configuration/registry files (e.g. a wiki name+path registry). Confidence: 0.8
- Prefers auto-setup by default — features like MCP init should be enabled automatically when initializing a personal wiki, not exposed as an opt-in flag. Confidence: 0.8
- Prefers a unified guided CLI: a single entry command (`llm-wiki init`) that walks through options interactively, rather than requiring separate subcommands/flows. Confidence: 0.85
- Prefers strict human-scoped AI boundaries: AI tools should only research and contribute to a human-designated wiki; never auto-ingest a wiki ("AI proposes, human decides"). Confidence: 0.85
- Prefers cross-scope discoverability with fallback: allow targeting a specific wiki scope but also support searching across related scopes including the personal wiki. Confidence: 0.75
- Requires Python 3.10 compatibility as a hard floor: never use 3.11+-only stdlib (e.g. `tomllib`) without a `try/except ImportError` fallback to the backport (`tomli`), and declare the conditional dep (`tomli>=2; python_version < "3.11"`) in BOTH `pyproject.toml` and the base `requirements.txt` so the CLI venv and the global base venv stay consistent. Confidence: 0.8
- Prefers enforcing invariants at the schema/protocol level rather than with runtime guards: a required argument in the tool function signature (so the MCP/JSON-Schema `required` list rejects the call) is preferred over an `if not param: return _err(...)` check inside the body. Confidence: 0.8
- Wants a session summary written as a markdown doc in the project repo at `docs/session/session-<summary-name>-<date>.md` — kebab-case descriptive slug + `YYYY-MM-DD` date suffix (e.g. `session-tier1-tier2-okf-apply-2026-09-01.md`). The user also asks for it explicitly ("hãy tổng hợp lại những gì đã làm (thay đổi file, ...)") and expects it even when the session changed no repo code (e.g. machine/tool config edits): summary must be change-centric — files modified/created/backed up with full paths, what could NOT be done via CLI, and the one manual step left for the human — delivered both as the doc and as a short chat recap. Before writing, re-read an existing `docs/session/` file so the new doc matches the established house format. Confidence: 0.95
- Session summary doc structure (accepted format): header with date/source/plan reference/status, key decisions made, detailed implementation walkthrough per phase with concrete file paths, a numbered verification checklist (each check + result), a files-changed list split into modified vs new, and operational notes (commit status, branch name, follow-up steps needed after pull, gotchas hit during testing like missing env vars). Written in Vietnamese. Confidence: 0.7
- Session docs are LIVING files, not write-once artifacts: when work continues after the doc exists, amend that same `docs/session/session-*.md` (fix stale counts like "files changed"/number of verification items, add new subsections) instead of creating a second doc. The user triggers this with a bare "hãy lưu những gì đã làm vào session docs" and expects the delta since the last write to be captured. Also expects two concrete sections: (a) "trạng thái máy này" — which manual setup/upgrade steps have actually been run on this machine vs still pending for the user, naming the affected instances, and any side effects that occurred (e.g. test artifacts written into real user config) plus how they were cleaned up and the code-level follow-up they imply; (b) a copy-pasteable reproduction block of the exact commands used to run the checks. Unflattering or inconclusive measurements are recorded as-is, not smoothed over. Confidence: 0.75
- Uses an AI CLI tool stack on macOS: opencode (config `~/.config/opencode/opencode.json`, auth keys in `~/.local/share/opencode/auth.json`, server logs in `~/.local/share/opencode/log/`), headroom as a proxy wrapper invoked as `headroom wrap <tool> ...` (installed via uv as `headroom-ai`, proxy log at `~/.headroom/logs/proxy.log`), and caveman (`@caveman-ai/cli` npm global under nvm, state in `~/.caveman/`, opencode plugin at `~/.config/opencode/plugins/caveman/`). Confidence: 0.75
- Checks per-session whether the headroom proxy actually wraps a given AI CLI session: proxy listens on 127.0.0.1:8787 (stats at `http://127.0.0.1:8787/stats`). A session only routes through it if launched via `headroom wrap <tool>` or with `ANTHROPIC_BASE_URL` set to the proxy — an inherited `OPENAI_BASE_URL` pointing at the proxy is NOT sufficient for this CLI's traffic (verify via `api_requests` in `/stats`, not just the process running). Confidence: 0.6
- Command Code config conventions (providers.json baseURL edits for the headroom proxy, `.bak-<purpose>` backup-before-edit habit, BYOK model-selection limits, UA-block vs proxy-broken diagnosis) are recorded in `commandcode-workflow/taste.md`. Confidence: 0.7
- Default opencode model is `opencode-go/mimo-v2.5` (set after the broken `openrouter/free` default; gotcha: a provider ID containing `/` fails model resolution and surfaces as a misleading "API key not recognized" error). Confidence: 0.7
- When replacing a default behavior/algorithm that real users depend on, keep the superseded version reachable behind a one-line config switch (e.g. `fusion = "weighted"`) instead of deleting it — it doubles as the rollback path AND as the measurable baseline the new default must beat in an A/B comparison. Ship the change with explicit upgrade steps for existing installs. Confidence: 0.75
- Treats silent degradation as worse than a crash: every fallback that turns a component off must be detectable — warn once per process, expose an error registry the caller/eval prints ("channel disabled because of an error", distinct from "disabled by config"), have dry-run modes report "index not built yet", bump a schema version so the next run warns, and have config-display show the truly effective value (applying/casting env overrides) instead of the default with an `[env]` label. Numbers that look valid while a part is dead are a bug. Confidence: 0.8
- Dependency-averse core: keeps Python pipeline code deterministic and free of new third-party/model deps, and pushes fuzzy, model-shaped steps (translation, LLM rerank) into the skill/prompt layer where the host AI tool's own LLM does the work. Heavy deps (e.g. an ONNX cross-encoder) are explicitly deferred to the roadmap rather than added to make a feature complete. Confidence: 0.8
- Wants costly features gated on measurement, never on intuition: build a small eval harness (P@k / R@k / MRR over a golden query file the human fills in from real usage) and enable the feature only when the delta is clearly positive. The tool must exit 0 with instructions when the golden set is empty rather than auto-inventing queries or numbers — AI-guessed benchmarks are called out as worthless. Confidence: 0.8
- Closes a work session by asking "tôi cần làm gì tiếp theo" and expects a handoff answer, not a recap: a short list ordered by value (naming the single bottleneck step first), each remaining manual action given as exact copy-pasteable commands for the specific instance it applies to, preceded by a live check of real on-disk state (schema/version markers, whether the human already ran a step, file/skill timestamps) so the answer distinguishes "already done on this machine" from "still pending" instead of repeating generic instructions. Secondary/non-urgent items are listed last and explicitly marked as not urgent. Confidence: 0.6
- Non-default config values must be self-documenting at the point of decision: when a flag is flipped (e.g. `vector = true`), the config file itself carries an inline comment block with the measured evidence (per-profile P@k/R@k/MRR numbers, date of measurement, where the history file lives), the concrete mechanism that justified it (which failure case the flag fixes), and the cost/trade-off accepted (latency, model load). Rationale: "ai sau mở ra sẽ biết vì sao, không phải đoán" — a bare value is a liability. Confidence: 0.75
- Validate the artifact before quoting its numbers, and never leave the user's state mutated by testing: check timestamps and diff on-disk content against the index/derived-state hash map; if the artifact was built by older rules (e.g. a stale vector index still containing excluded files) rebuild it and re-measure rather than reporting numbers on a dirty base. When a rebuild needs a temporary config flip: `cp <file> <file>.bak-<purpose>` → edit → `mv` the backup back → `grep` the key to prove it is restored (the tool's compare/eval mode should be able to force profiles without editing config). Purpose-named backup suffix, not generic `.bak`. Confidence: 0.75
- When a measurement regresses, decompose it before blaming a bug: dump each component's own ranked list (per-channel ranks + fused order) so the mechanism is visible, then sweep the suspected parameter and let the data decide — including against the agent's own hypothesis ("`rrf_k` không phải đòn bẩy — sweep 20→250 cho số liệu giống hệt; tôi đoán sai, data phủ định"). Refuted hypotheses are written into README/session docs as counter-facts, and the sweep script is kept as a reusable measurement (proposed for promotion into a scripts dir) because it settles in 30 seconds what a reading session would not. Confidence: 0.7
