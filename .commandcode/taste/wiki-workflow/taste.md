- **MCP server tools intentionally cover read + intake only, NOT direct wiki write**: tools for reading (`wiki_search`, `wiki_read`, `wiki_list`, `list_raw_source`, `read_raw_source`, `semantic_search`, `wiki_lint`) and one write into staging (`wiki_submit` → `raw/inbox/`) + one proposal staging (`wiki_propose_edit` → `wiki/.proposals/`). Indexing/ingest is maintainer-only via CLI. This separation keeps AI contributors as **sources of content**, not silent editors of canonical wiki. Confidence: 0.95
- **Use `EnterPlanMode` tool for non-trivial refactors** that touch schema + tooling + skills + bootstrap scripts (4+ subsystems). Even before plan mode, do **explore-phase parallel subagents** (4 concurrent `agent` calls with `subagent_type: explore`) to inventory: structure mapping, schema rule extraction, existing template/scaffolding patterns, MCP/skill surface. Then `ask_user_question` for ambiguous trade-offs with `(Recommended)` on the preferred choice, THEN write the plan to a markdown file (`~/.commandcode/plans/<slug>.md`). Plan structure: Goal → Quyết định đã chốt table → Phase 0/1/2/3 numbered → Files tạo/sửa/xoá → Out of scope → Risk+mitigation table. Auto-exit plan mode lets the user accept and start implementation. Confidence: 0.9
- After a plan is written (plan mode), the user wants to review the full plan content in chat before approving implementation — right after auto-exit plan mode they asked "cho tôi xem lại plan" instead of accepting blind; surface the complete plan text (or a faithful summary of it) and invite scope adjustments before starting any code. Confidence: 0.6
- **Centralize hard-coded paths in a single `tools/paths.py` module** (or equivalent) rather than letting `wiki/`, `raw/`, `rag/.rag_index/`, `wiki/.proposals/` be scattered across 8+ files. Every env-configurable path lives there with sane defaults derived from `__file__` location, plus env-var overrides (`WIKI_ROOT`, `WIKI_DB`, `WIKI_DIR`, etc.). Refactor 8 call sites (db.py, mcp_server.py, watch.py, reindex.py, clean_sources.py, rag/index.py, rag/search.py, scripts/common.py) to import from paths.py. Confidence: 0.9
- **Resolution priority for `WIKI_ROOT` in global-base setup** (order matters): (1) `WIKI_ROOT` env var set by CLI wrapper, (2) cwd if it contains a `wiki/` directory, (3) parent of `tools/` as fallback for legacy in-wiki layouts. After splitting runtime vs data, `tools/paths.py` in the global base must NOT re-export from `llm_wiki.paths` (the package version) — otherwise the global venv's installed `llm-wiki` package shadows the local `tools/paths.py`. Use a local-only definition in `tools/paths.py` that resolves `WIKI_ROOT` per the priority above. Confidence: 0.85
- **Wiki project-wiki scoping is single-tenant by default: 1 repo = 1 project-wiki**, NOT multi-tenant. Multi-tenant (1 repo = N projects via `project:` field) requires ~6 changes across DB/search/MCP/skills and is rejected as too complex for the per-project fork workflow. `domain` (top-level folder) remains the only scope axis. Confidence: 0.9
- **When converting a working wiki instance into a reusable template repo**, split into 2 repos: template (`llm-wiki-base`) + working instance. Template ships only generic schema/tooling/skills/init — never personal domain content. Instance keeps its content and receives the infrastructure refactor (paths.py, .gitignore fix, .base rename). Use `cp -r` from instance to template, edit template, then `cp` back to instance for parallel evolution. Confidence: 0.85
- **Ship a one-shot `init.sh` bootstrap for fresh template clones**: idempotent steps (create .venv, mkdir staging dirs with .gitkeep, copy skeleton wiki/{index,log}.md, link agent skills, copy .env.example → .env). Re-runnable — each step checks before acting. End with a clear "Next steps" hint listing the 4-6 commands to get started. Confidence: 0.85
- **CLI distribution for the wiki: prefer a real Python package (`pip install .` → `llm-wiki` command) over shell scripts**. Use Typer for the CLI surface; entry point declared in `pyproject.toml` (`[project.scripts]`). Bundle skill definitions and skeleton templates as package data (under `src/llm_wiki/skills/` and `src/llm_wiki/templates/`) so `pip install` produces a self-contained, cross-platform install. Pipeline source (`tools/`, `rag/`, `scripts/`) is also bundled as package data so `init personal` works from any cwd (not just from the repo root). Confidence: 0.85
- **Cross-platform user-config support is a hard requirement for the install step** (Windows + macOS + Linux). Use `platformdirs` or a hand-rolled equivalent: macOS → `~/Library/Application Support/`, Linux → `$XDG_CONFIG_HOME` (default `~/.config/`), Windows → `%APPDATA%`. Always provide a `~/.{client}/` fallback for legacy paths. Symlink skills on macOS/Linux; fallback to `shutil.copytree`/`copy2` on Windows if symlink fails (no Developer Mode). Ship a smoke test that exercises all 3 OSes before declaring the installer done. Confidence: 0.9
- **Translation/expensive-LLM features should delegate to the host AI tool's LLM by default** (Claude Code → Claude, OpenCode → provider config). Treat external API keys (`LLM_WIKI_TRANSLATE_API_KEY`) as OPTIONAL bypass, not the primary path. Always support `--dry` preview before calling the API. The CLI banner must warn about extra token cost (e.g. when `len(langs) > 5` or N pages > 20) BEFORE invoking skills. Skill prompt must forbid paraphrasing, code-block translation, and frontmatter mutation — preserve 100% structure. Confidence: 0.9
- **Drop legacy skill trees entirely rather than maintaining parallel versions**. When a new `.agents/skills/llm-wiki-{ingest,query,lint}` tree supersedes an old `skills/{wiki-contribute,wiki-research}` tree, remove the old one — don't ship both with a "legacy" note. One skill tree per repo, no deprecation noise. Confidence: 0.85
- **Ship `.example` files for user-specific state** (`.serena/project.yml.example` with `<your-project-name>` placeholder, MCP sample configs with `*.mcp.sample.json` extension). Never commit the real `.serena/project.yml` or actual MCP config — those are per-instance. Confidence: 0.85
- **Gitignore default for `raw/` and `raw/inbox/`** with explicit `!raw/.gitkeep` and `!raw/inbox/` exceptions to preserve folder structure. Allows `git add -f raw/<file>` to pin individual sources. Document this in AGENTS.md so user knows raw is locally-cached, not committed. Confidence: 0.9
- **Package-related bug fixes ship alongside the template refactor**, not deferred. When moving to template form, fix in the same pass: (1) `.gitignore` `config/` → `config/*.local.json` to allow shipping sample configs, (2) rename user-named config files (`nihwihome-wiki-base.base` → `templates/obsidian-base.base` so Obsidian doesn't auto-load + neutralises the personal name), (3) move hard-coded `.serena/project.yml` to `.example`. Confidence: 0.85
- **Verify template with smoke test before declaring refactor done**: run `bash init.sh` → drop test source into `raw/inbox/` → run `tools/watch.py` ~18s → verify file moved to `raw/` → verify `wiki/log.md` got entry → verify `db.search.hybrid_search()` returns the new page → verify `lint.lint()` reports no findings → verify MCP server exposes all 9 tools + 2 resources via `mcp._tool_manager`. Cleanup test artifacts before final. Confidence: 0.85
- **When repo directory is renamed, recreate the Python `.venv`** — venv hard-codes absolute paths in `pyvenv.cfg` and activation scripts. Move dir → `rm -rf .venv` → re-run `init.sh`. Don't try to patch the venv in place. Confidence: 0.8
- **In `ask_user_question` options, label the preferred choice with `(Recommended)`** to guide the user toward the pre-decided approach. Used when there's a clear winner among 3 alternatives based on prior context — saves the user from re-evaluating trade-offs they've already seen. Confidence: 0.8
- **3-object wiki data model (was 4, collapsed after re-evaluation)**: `raw/inbox/` (staging, mutable) + `raw/` (local cache, gitignored, can be dropped — URL + no-URL both go here) + `wiki/` (LLM-owned). `archived/` layer was removed because once content has been curated into wiki, a separate no-URL archive is redundant. The URL/no-URL distinction lives only in the `sources:` field. Confidence: 0.95
- **`raw/` is an optional local cache, NOT immutable "source of truth"** (diverges intentionally from Karpathy). The primary truth is the URL in `sources:` (hosted by third party, survives user deletions). `raw/` is gitignored by default; user can `git add -f raw/<file>` to pin important sources. Rationale: knowledge base stays portable + disk-light; URL re-fetch covers verification. Tooling never auto-deletes raw — only the user does. Confidence: 0.9
- **Linter must catch copied-state violations specifically**: rules like `sources-no-local-path` (block `raw/inbox/...` in `sources:` field — staging path drifts; `raw/` outside inbox is user-managed so it's allowed) and `body-no-raw-inbox-wikilink` (block `[[raw/inbox/...]]` inline; `[[raw/...]]` outside inbox is allowed). Rule names updated to reflect narrower scope. Confidence: 0.9
- **Embed path to `tools/` from `rag/*.py` at import time** so `python rag/index.py` works from repo root: `sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "tools"))` before importing `paths` or `embed`. Same for scripts that import from `tools/`. Avoid `PYTHONPATH=...` as the only way to run. Confidence: 0.8
- Uses Karpathy's "LLM Wiki Compounding Rules" as **a starting reference, not gospel**. When reasoning about wiki structure, ingestion, or schema, consult Karpathy's invariants (wiki self-contained, schema co-evolves with LLM behavior, copied-state rule) but **willingly diverge** when a more pragmatic model emerges from the user's domain (e.g. dropping immutable raw/ for portability + disk pressure). Document divergences explicitly in AGENTS.md so the user can audit them. Confidence: 0.9
- When a wiki/spec violation is discovered, expects the fix to include updating the schema (`_schema.md`), the ingestion skill, and the pipeline so the violation cannot recur — not just patching the affected files. Lint rules are preferred enforcement mechanism over documentation alone. Confidence: 0.9
- Distinguishes between "copied state" (volatile paths/SHAs/counts that will drift) and "git-tracked local references" (paths to files in the same repo, which git guarantees exist). Allows local paths in `sources:` as legitimate provenance when the file has no public URL, but rejects mixed lists (URL + path) as redundant. Confidence: 0.85
- Prefers asking clarifying questions via `ask_user_question` for ambiguous edge cases (e.g. "no-URL case", "body refs policy") rather than guessing — even when the request seems clear, edge cases warrant confirmation before bulk-editing dozens of files. Confidence: 0.85
- Prefers plan mode with a written plan file (e.g. `clean-wiki-sources-raw-archived.md`) for non-trivial refactors touching schema + skills + many files. Plan should include: scope survey, transformation rules table, file change list, verification steps. Confidence: 0.85
- Expects one-shot transformation scripts to be idempotent and to support a `--dry` flag for previewing diffs before applying. Idempotency verified by running `--dry` twice. Confidence: 0.8
- Before applying a bulk transform from a plan, run a verification script (grep/count) to confirm the exact set of files the plan claims will change. If the actual count differs from the plan's estimate, re-survey and update the plan before applying — don't trust plan numbers blindly. Confidence: 0.85
- When a newly-added lint rule produces a false positive on the user's own artifacts (e.g. log entries, docs that describe the rule), refine the rule to exclude the false-positive case (e.g. skip code spans, skip rule-description text) rather than disabling the rule or adding the artifact to an ignore list. Confidence: 0.85
- Strong FACT vs JUDGMENT separation: AI writes FACT (description, summary, cross-ref) automatically with `status: active`; JUDGMENT (direction, "why" decisions, trade-offs) is ALWAYS marked `status: draft` until human approves — never pre-promote judgment to canonical. Confidence: 0.95
- Hard anti-fabrication rule: when source lacks evidence, leave blank or write to `alerts.md#open-questions`; NEVER invent REQ/ADR or content to fill gaps. Better an empty page with citation than a confident lie. Confidence: 0.95
- EVIDENCE ĐI KÈM (citation rule): every claim cites `[^sN]` pointing to raw, with verbatim 1–2 line quote directly below the footnote (secrets already scrubbed). Judgment claims cite human words (transcript/decision log); factual claims cite machine evidence (source/test). Confidence: 0.95
- COPIED-STATE rule: pages describe SHAPE of things, never quote dynamic values (SHA, HEAD, line counts, dates, config values, dep versions). Dynamic values live in frontmatter/traceability and are read live. Only historical narrative is allowed to be literal. Confidence: 0.9
- Review artifacts, NOT plans: for canonical layer (REQ/ADR), agent pre-generates a draft (status `draft`) and human reviews the actual `git diff` before approving — never approve a plan upfront. For low-risk technical observation, write directly. Confidence: 0.9
- Pin rule protects human manual edits: when regenerating a page, never let new source silently delete content a human added — if newer evidence conflicts, push to `alerts.md#pins-in-conflict` for human decision; if anchor disappears, mark `status: orphan` not delete. Confidence: 0.9
- Multi-agent safety: RESERVE ID/path in `registry.yml` with `status: planned` BEFORE writing a new page; subsequent sessions seeing `planned` switch to `update` instead of `create` (prevents forks like duplicate REQ under different names). LLM reasoning lives outside all locks — only the registry write is the claim. Confidence: 0.85
- Progressive disclosure + union retrieval: agent starts at INDEX.md, follows ≤3 related pages per turn, never loads the whole wiki. Union of (pages from INDEX/links) and (top-k grep on bodies) — additive, never worse than index-only. Below ~100k tokens, NO vector DB needed. Confidence: 0.85
- **Domains must NOT be hardcoded in the schema.** Domain is auto-detected by the LLM when ingesting raw — never fix a single `domain: jvntoolkit` constant in SCHEMA. Schema must remain domain-agnostic so the same wiki can hold multiple projects/knowledge bases. Confidence: 0.95
- **Wiki structure is sub-foldered by detected domain**, not by topic area only. Layout: `.wiki/topics/<domain>/<area>/...`, `.wiki/traceability/<domain>-req-to-code.yml` + `<domain>-code-to-req.yml`, `.wiki/raw/<domain>/<file>.md`. ID format includes domain prefix (`REQ-<domain>-*`, `ADR-<domain>-NNN-*`). Confidence: 0.9
- **Wiki skeleton starts EMPTY** — only meta files (SCHEMA/INDEX/overview/current-state/log/registry/pins/alerts) and empty folder placeholders (`.gitkeep`). DO NOT pre-populate by mirroring existing repo docs into `topics/<domain>/...` — that is the intake skill's job once raw is dropped. Confidence: 0.95
- **Raw evidence is MOVE-on-ingest, never auto-delete.** Workflow: human drops raw into `.wiki/raw/inbox/`, LLM detects domain → writes page → cites raw → moves file to `.wiki/raw/<file>.md` (no-URL sources go here too, no separate `archived/`). User may later delete `raw/` files freely (gitignored, treated as local cache); tooling never deletes raw. Optional long-term pin via `git add -f raw/<file>`. Confidence: 0.95
- **Each wiki page carries `domain` in frontmatter**, filled by LLM during ingest. Cross-domain pages use multi-value `tags:` plus single primary `domain:` — no need for special cross-domain folder. Confidence: 0.85
- **Ingest workflow is human-driven**: human drops raw into `inbox/`, then invokes LLM intake (or skill `llm-wiki-ingest`). LLM does NOT auto-poll the inbox — the trigger is explicit. Confidence: 0.85
- **Wiki operations need their own skills in `.agents/skills/`** with `SKILL.md` + `scripts/<tool>.py` — not just docs, but runnable CLI tools that any LLM agent can invoke via slash command. Operations: `llm-wiki-doctor` (lint), `llm-wiki-search` (query), `llm-wiki-ingest` (intake), `llm-wiki-publish` (session-end). Confidence: 0.9
- **Wiki skill scripts must be stdlib-only Python** (no PyYAML, no requests, no numpy) — must run in any environment without `pip install`. Manually re-implement YAML parsing, BM25 search, etc. using only `re`, `pathlib`, `json`, `collections`. Confidence: 0.9
- **Session-end publishing has a 2-stage human gate**: every LLM session ends with a `llm-wiki-publish` step that writes proposals to `.wiki/.session/<session-id>/<slug>.md` (NOT directly to wiki canonical). Human must explicitly `apply` (move to `inbox/`) or `reject` before the insight ever touches the wiki. Stronger than REQ/ADR gating — applies to ALL session learnings, not just canonical ones. Confidence: 0.95
- **Before bulk-writing wiki pages from raw, propose a takeaway + ask human clarifying questions** on structural choices (page split, table format, classification). Even for low-risk observation pages, get sign-off on the structure before generating dozens of files. Confidence: 0.9
- **Do NOT mirror stale/legacy raw content as if it were current truth.** If raw self-identifies as superseded (e.g. "LEGACY — current version has 59 items not 19"), write a `note` page explaining the limitation + link to canonical source, instead of mirroring the stale content verbatim. Confidence: 0.9
- **Wikilinks must include `.md` extension**: prefer `[[path/to/page.md]]` over `[[path/to/page]]`. Wikilinks without extension cause silent broken links. Also separate `#anchor` from path in resolution (lint must strip `#anchor` before checking file existence). Confidence: 0.85
- **Skill coverage parity between personal and project bases is expected**. If personal base has `ingest` + `lint`, project base should have the equivalents (`wiki-project-ingest`, `wiki-project-lint`) even when project base has unique skills (`wiki-project-research`, `wiki-project-plan`). User notices gaps ("project skills thiếu ingest, lint") and expects full coverage so project-wiki isn't a degraded subset. Companion skills should explicitly cross-reference each other in their descriptions. This parity extends beyond skills to shared MECHANISMS: user explicitly corrected the tier-1/2 plan when the verify/trust workflow was scoped to personal only ("verify ở cả personal và project thay vì chỉ verify mỗi personal") — trust fields (`generated`/`verified`), review/consolidate skills, and the verify CLI all apply to BOTH profiles; any new mechanism defaults to both-profile scope unless the user says otherwise. Confidence: 0.95
- **Project-specific ingest/lint adaptations must differ from personal in documented ways**: project ingest drops the "Domain languages" exception (no vocab/grammar), uses `tech-stack/architecture/convention` style domains, references code paths as primary anchors (not topics), and project lint adds a stale-vs-code check (grep `entity` + `concept` page code paths against codebase, flag stale — never auto-fix, always human). Document the difference inline so the 80% copy-paste feels intentional. Confidence: 0.85
- **Per-wiki user-level config lives in `.llm-wiki.toml` at wiki root** (TOML, parsed via `tomllib`/`tomli`), NOT in `.env` (which is reserved for tooling internals). Schema: `[translate] enabled = true, langs = ["vi", "ja"]`. CLI surfaces this as `llm-wiki translate enable/disable/status`; the config is read by ingest skill to trigger auto-translation. Empty/missing = no auto-translate (silent skip, not an error). Confidence: 0.85
- **Config layering: behavior config is per-wiki and committed to the repo; global scope is for registry/routing only.** `.llm-wiki.toml` (wiki-specific behavior: retrieval weights/chunk_tokens, review cadence, lifecycle, lint) stays at the wiki root and is COMMITTED — a `git clone` to another machine reproduces the same behavior. `.env` stays gitignored (machine-local paths/secrets); when new config sections land, init's `.gitignore` template must keep this committed-vs-gitignored split explicit. The global dir (`~/.llm-wiki-base/`) holds only `registry.toml` (MCP name→path routing) + runtime/venv — never behavior config; if shared machine defaults are ever needed, precedence is `per-wiki > global > built-in`. Confidence: 0.75
- **Translated wiki pages use `<slug>.<lang>.md` pattern** with `TRANSLATED_SUFFIX_RE = r"\.[a-z]{2,3}\.md$"` to detect. Translated files: (1) share frontmatter keys with source (provenance intact), (2) preserve heading structure 1:1 (lint `translate check` enforces), (3) are **skipped from DB/RAG** in `tools/{ingest,reindex,watch}.py` + `rag/index.py`. Confidence: 0.9
- **Skills live per-wiki, NOT in user-global AI-client dirs** (e.g. NOT `~/Library/Application Support/claude/skills/` or `~/.config/opencode/commands/`). On `llm-wiki init personal|project`, skills are copied into `<wiki_root>/.agents/skills/<name>/SKILL.md` (universal) or `<wiki_root>/.claude/skills/<name>/SKILL.md` (Claude-specific). For Claude target, also symlink to `.agents/skills/` so cross-tool compatibility is preserved. Skills in `llm-wiki-base/src/llm_wiki/skills/` are ONLY sources for the init copy step — they are never used directly at runtime. This supersedes the earlier global-install approach: each wiki owns its own skill copies, so deleting the template repo doesn't break wikis, and 2 wikis on the same machine can have different skill versions. Confidence: 0.9
- **`llm-wiki init` must take a `--skills-target` flag** (options: `universal` | `claude` | `both`, default `universal`). Controls whether skills land in `.agents/skills/` (default — most portable across AI tools), `.claude/skills/` (Claude-specific), or both. Skill copy happens AUTOMATICALLY for both `init personal` AND `init project` — no opt-in flag needed; the user opts in by running `init` at all. Confidence: 0.85
- **Per-wiki skills are copied (not symlinked) from the package source** — `shutil.copytree` of the skill folder (SKILL.md + optional scripts/) into the wiki's `.agents/skills/`. Symlinks to template source are rejected because: (1) deleting the template repo breaks the wiki, (2) copying is more cross-platform (no symlink permission needed on Windows). The wiki becomes self-contained; template upgrades only flow through explicit re-init or a future `llm-wiki skills update` command. Confidence: 0.85
- **Skills must be rewritten for per-wiki scope**, not the global template path. Skill content (paths, MCP server references, ingestion commands, etc.) must point to `<wiki_root>` paths resolved via env/cwd, not to absolute template-repo paths like `/Users/nihmtaho/.../llm-wiki-base/`. The skill runs in the AI client that opened the wiki dir, so paths like `<wiki_root>/wiki/`, `<wiki_root>/raw/inbox/`, `<wiki_root>/.env` are correct — not template-relative ones. Confidence: 0.85
- **For test/validation work on the wiki tooling, create test wikis under `/Users/nihmtaho/developer/repos/per-projects/`** — that's the user's designated scratch workspace. Naming convention used so far: `test-personal-wiki/` (in-place) and `test-project-wiki/project-wiki/` (subfolder for project base). Each test wiki should be data-only. When the test would touch SHARED global state (the base `registry.toml`, MCP config), point the tool at a throwaway base dir via its env override (`LLM_WIKI_BASE_DIR=<scratch>/t3base`) instead — `llm-wiki init` auto-registers into the user's real `~/.llm-wiki-base/registry.toml`, so a careless test pollutes it; if that happens, `llm-wiki wiki remove <name> --force` each test wiki and delete the scratch dirs before finishing. Confidence: 0.85ding structure 1:1 (lint `translate check` enforces), (3) are **skipped from DB/RAG** in `tools/{ingest,reindex,watch}.py` + `rag/index.py`. Confidence: 0.9
- **Agent config files (`_schema.md`, `AGENTS.md`, `CLAUDE.md`) must be included in the init scaffolding** — bundled as package data under `src/llm_wiki/templates/agents/` and copied to the wiki root during both `init personal` and `init project`. These are foundational for agent collaboration and should never be missing from a freshly-initialized wiki. Confidence: 0.9
- **Remove redundant components outside `src/` when source is reorganized into a `src/` layout** — in `llm-wiki-base`, after source code moves into `src/`, anything duplicated or obsolete outside it (e.g. legacy top-level modules mirroring what now lives under `src/`) should be deleted rather than left as stale legacy. Keeps the repo single-source-of-truth and matches the package-data bundling model where skills/templates ship as package data under `src/llm_wiki/`. Confidence: 0.9

## New from concrete session implementation (`.agents/skills/llm-wiki-*` on jvntoolkit repo)

The abstract taste entries above cover the template design; the following cover the **actual CLI surface** exercised and verified in this session, which diverges in specifics.

- **`llm-wiki-publish` CLI surface is `propose | list | show | apply | reject`.** `apply` moves a proposal file `.wiki/.session/<session-id>/<slug>.md` → `.wiki/raw/inbox/<slug>.md` (for later ingest — it does NOT write the wiki page directly); `reject` records the human's reason in the session manifest. The session-id folder + manifest INDEX.md is created on first `propose`. Confidence: 0.85
- **Per-session manifest at `.wiki/.session/<session-id>/INDEX.md`** tracks every proposal as a row with a status column (proposed / applied / rejected / pending). Each proposal is its own `<slug>.md` file; the manifest is the single source of which proposals exist and their review state. Confidence: 0.9
- **`llm-wiki-doctor` has explicit severity tiers and flags**: `--only <comma-list>` (broken-links, orphan, frontmatter, id, registry, raw-stale, raw-orphan), `--fix` (safe auto-fix only), `--emit-alerts` (append findings to `alerts.md`), `--json`. Exit codes: `0` clean, `1` critical (broken wiki/md links incl. unresolved wikilinks, duplicate ID in same domain, missing frontmatter, registry-declared-but-missing file), `2` warn (orphan page, inbox raw older than 7 days, uncited raw under `raw/<domain>/`). Never auto-deletes pages or edits `human-verified` claims. Confidence: 0.9
- **Doctor must split `#anchor` off wikilink/path before existence check** — `[[alerts.md#open-question-...]]` resolves the path `alerts.md` and treats the anchor as in-page; this avoids false-positive broken-link reports (verified bug fixed mid-session). Confidence: 0.85
- **`llm-wiki-ingest` is a two-script pair, not one**: `ingest.py` has subcommands `plan` (scan `inbox/`, emit per-file JSON+markdown with `domain_signals` = regex-match list + weights, `filename-prefix` hint as lower-weight fallback, `classification` = `source|concept|req-candidate|adr-candidate`, `content_preview`, `suggested_target`; also emits existing/new domain lists) and `status` (print inbox + `raw/<domain>/` catalog as JSON). `move.py` validates source∈`inbox/`, target∈`raw/<domain>/`, and rejects if the target file already exists (atomic rename on same filesystem); it also appends to / rewrites `registry.yml#raw.{inbox,by_domain}`. The LLM, not the script, decides domain + writes the page — tools only scan/move/catalog. Confidence: 0.85
- **Domain detection emits weighted signals rather than auto-deciding**: `ingest.py plan` returns `suggested_domain` + a list of matched regex patterns each with a weight + filename-prefix hint, but marks it as a hint (`⚠ LLM phải quyết` when no confident signal). The LLM reads `content_preview` + signals and makes the final call; scripts never override an empty/ambiguous guess. Confidence: 0.85
- **Classification categories used by ingest**: `source` (raw evidence → mirror to `topics/`, `kind: source`), `concept` (observation/technical → `kind: concept`, may be written directly), `req-candidate` (product intent → propose `REQ-<domain>-*`), `adr-candidate` (architectural decision → propose `ADR-<domain>-*`). Detected via header regexes mentioning REQ/ADR/requirement/decision/要件/決定. Confidence: 0.8
- **`llm-wiki-search` BM25 scoring**: title tokens ×3 weight, path tokens ×2 (path also split on `/` for sub-token match), body once; `make_snippet` returns the first window containing any query term (not the head) so snippets are topically relevant; flags `--path <p>` (list inbound links across wiki), `--count <term>` (page count), `--include-raw`, `--snippet-chars N`, `--domain`, `--kind`, `--json`, `--top`. Pure stdlib; no vector DB. Confidence: 0.85
- **Wikilink style preference (verified mid-session)**: intra-folder links use bare `[[filename.md]]`, cross-area links use relative `../../../alerts.md#anchor` (depth depends on nesting), never full `topics/<domain>/...` paths from inside a sibling — those silently break. The doctor lint now rejects both missing `.md` and unresolved relative wikilinks. Confidence: 0.8
- **Base runtime deps separated from CLI deps into a dedicated `requirements.txt`**: runtime dependencies for the centralized MCP server (mcp, fastembed, numpy, trafilatura, PyMuPDF, youtube-transcript-api) live in `src/llm_wiki/requirements.txt` at the package source and are installed by `llm-wiki base install` into the global base venv (`~/.llm-wiki-base/.venv/`). CLI dependencies (typer, rich) remain in `pyproject.toml`, installed via `pip install -e .` in the main venv — never in the base venv. This keeps the centralized MCP server's runtime environment minimal and separate from the CLI's environment. Confidence: 0.85
- **Centralized MCP server uses `LLM_WIKI_BASE_DIR` env var** (not `WIKI_ROOT`) to locate the global base directory (`~/.llm-wiki-base/`). The CLI wrapper sets this when launching the server; the server reads `registry.toml` directly via `tomllib` — it does NOT import `registry.py` (that module is CLI-side only). This distinguishes the centralized server's base-dir resolution from per-wiki `tools/paths.py` which may still use `WIKI_ROOT` for wiki-relative path resolution. Confidence: 0.8
- **Verify an MCP server over the real protocol, not just by importing the module**: drive it with raw JSON-RPC on stdio in the correct sequence (`initialize` → `notifications/initialized` → `tools/list` / `tools/call`) using the base venv's python, then assert (a) server name, (b) per-tool `inputSchema.required` (e.g. `wiki` required on write tools, optional for cross-scope on read/search tools), (c) end-to-end side effects of a write tool (file lands in `raw/inbox/`, `wiki/` untouched), (d) the negative case (call missing the required param → error). Reflective inspection of `mcp._tool` internals or `inspect.getmembers` is a weaker check and gets replaced by the protocol test. Finish by cleaning up test wikis/temp artifacts and `py_compile`-ing every modified source file. Confidence: 0.85

## New: idea-evaluation workflow (docs/human-ideas)

- **User keeps personal improvement ideas as structured markdown bundles under `docs/human-ideas/<topic>/`** (e.g. `my-idea/` with `overview.md`, `idea.md`, `index.md`, `wiki.config.toml`, and per-skill sub-files like `skill-idea/ingest.md`, `query.md`, `lint.md`...). When the user points at such a folder, read ALL files in the bundle before evaluating — the ideas are split across many small docs, not one file. Confidence: 0.6
- **When asked to evaluate ideas against a codebase ("đọc và đánh giá các phương pháp có thể apply"), inventory the CURRENT implementation state first via an explore subagent, then compare.** The survey prompt is structured and strictly factual: per-area bullets with file paths, quote key schema/code snippets briefly (frontmatter template, DB schema, lint-check list, chunking params), and the explicit instruction "list what EXISTS today, not what's missing". Only after this baseline does the agent judge which proposed methods are applicable. Confidence: 0.7

## New: tiered feature adoption + config-architecture constraints (OKF bundle implementation)

- **Adopt improvements in tiers: implement high-value, architecture-compatible tiers immediately; defer speculative/heavy tiers to a roadmap doc in `docs/`** (user instruction: "Lưu tier 3 vào plan trong `docs/`"). Deferred items are written up as a Vietnamese roadmap doc (format matching the idea bundles), never half-implemented or left as code TODOs. Confidence: 0.8
- **Per-call config/env resolution in the centralized MCP server — never read env or `.llm-wiki.toml` at module level.** The server calls `_set_wiki_ctx()` to set per-wiki env before each tool call, so any config read must happen inside the function on every call. Module-level constants (e.g. `BM25_WEIGHT` in search.py) are a bug pattern to eliminate when migrating config into TOML. Confidence: 0.85
- **New shared modules need a self-contained shim for the global base venv**: scripts under `base_tools/`/`base_rag/` run from `~/.llm-wiki-base/.venv` which does NOT have the `llm_wiki` package installed, so any new shared module (e.g. config_file.py) gets a `base_tools/` shim following the existing `paths.py` pattern — try `from llm_wiki.<mod> import *`, fallback to an inline copy of the needed (read-only) parts; write-only pieces stay package-side. Because the two copies are deliberately duplicated, every new config key or path constant must be added in BOTH places in the same edit (`config_file.py` DEFAULTS + `base_tools/config_file.py` DEFAULTS, `paths.py` + `base_tools/paths.py`) — half-updating is the classic failure mode here. Confidence: 0.85
- **Human approval of artifacts = `llm-wiki verify` CLI (shared personal + project)**: `llm-wiki verify <path> --by <human-id>` writes `verified: {by: "human:<id>", at: <ISO-8601>}` as a deterministic frontmatter edit (no LLM); `--unverify` removes the field (page degrades to unverified pending re-review) and does NOT require `--by`. AI never sets `verified` (ingest writes only `generated`); when consolidate/review changes judgment content it must `--unverify` the affected page so human re-approves. Implementation gotcha recorded: in the Typer handler, import `verify` as a module (`from llm_wiki import verify as verify_mod`) — a function named `unverify` imported alongside a param named `unverify` silently shadows the flag. Confidence: 0.85
- **Lint = deterministic/structural vs review = semantic, strictly separated ("tất định trước, sinh sinh sau")**: `lint.py` only reports structural findings (broken-wikilink target existence, missing-frontmatter, status/confidence vocab incl. planned/deprecated/superseded, timestamp formats `updated`=YYYY-MM-DD & `stale_after`/`verified.at`=ISO-8601, footnote↔`sources[].id` match, stale-after-passed, missing-index-entry/domain-missing-index deduped per domain, pin-orphan, config-driven banned-terms/dense-bullet/indent-depth advisories) and auto-fixes only re-derivable state (`--fix` rebuilds `<domain>/index.md` additively, deletes dangling DB rows; never deletes pages or pins). Semantic checks (contradiction, stale claims, missing concepts, trust gaps, pin conflicts, stale code references) live in the review skills (`llm-wiki-review` / `wiki-project-review`) which write gaps to `wiki/alerts/<slug>.md` (`domain: alerts, kind: alert, status: open, last_seen`), auto-close after 2 consecutive passes without re-mention, cadence-gated by `[review].interval_days` + watermark `wiki/.review_state.json` (watch only prints a "[review] due" hint). Never merge semantic verdicts back into lint. Confidence: 0.8
- **Consolidate skills merge additively with distill-verify**: only raw→concept (never concept→concept regeneration), re-ground claims from raw with `[^id]` citations, citation set must NOT shrink across merges (count `[^id]` before/after), duplicates deprecate via `status: superseded` + `x_supersedes: <canonical-path>` with a pointer left (never delete files), respect `wiki/pins.yml` (re-check after merge), own watermark `wiki/.consolidate_state.json`. Confidence: 0.8
- **Reindex is incremental by content-hash by default — never full-rebuild-first**: DB rows carry a `content_hash` column and RAG chunks carry per-file hashes (vectors spliced via np.delete/vstack on change); `--check` dry-runs (lists files to index/delete + config drift vs `.index_meta.json`), `--full` forces rebuild (required after changing `chunk_tokens`/`embed_model`/`vector`); `retrieval.vector=false` (the default) skips RAG build entirely — BM25/FTS keeps working as the deterministic fallback. `llm-wiki config show` prints the effective config annotating each key's source (default/toml/env). FTS5 MATCH terms must be sanitized: SPLIT the query into `\w+` tokens (`re.findall(r"\w+", q)`) and re-join — do NOT strip non-word chars from each whitespace term, because unicode61 tokenizes `kumquat-xray-77` into `kumquat`/`xray`/`77` while a stripped `kumquatxray77` never matches, so every hyphenated identifier silently returns 0 rows. Retry chain per channel is raw query → AND of sanitized terms → OR of sanitized terms, and it must advance on **0 rows as well as on exceptions** (one Vietnamese stopword like "là"/"gì" kills the AND), with `relax_recall` gating only the OR step; return-early-on-first-attempt is the bug that made recall collapse. Confidence: 0.85
- **Retrieval quality is a committed, measured artifact**: golden queries live in `<wiki_root>/eval/golden.toml` (TOML — no YAML dep; it is DATA, so it is COMMITTED), while `eval/results.json` (derived, appended with a config fingerprint) is gitignored. `eval/` deliberately sits at the wiki ROOT and not under `wiki/`, because lint treats every non-hidden folder under `wiki/` as a domain and would report `domain-missing-index` on a freshly initialized wiki. `llm-wiki eval --compare` runs the old formula as baseline plus each new-channel profile and prints a verdict; enabling `retrieval.vector` requires a positive ΔR@k on that wiki's own golden set. Confidence: 0.75
- **Any behavior/config change must be propagated through the whole doc set in the same pass**, not left to a later cleanup: `templates/agents/{_schema,AGENTS,CLAUDE}.md` (runbook + config block + tool table), `README.md` (CLI list, config block, MCP tool table, plus a dedicated "upgrade from previous version" section when defaults change), `docs/{init,credits}.md`, and the tier roadmap file (move finished items to a "ĐÃ LÀM" section with the plan/session link, keep the deliberately-deferred part in the roadmap). Skill edits follow the same rule — apply to BOTH the personal and the project variant. Confidence: 0.8
- **When the user delegates a human-only judgment task to the AI ("giúp tôi điền vào đi"), the deliverable is a clearly-labelled CANDIDATE, not a filled-in answer.** For a golden eval set this means: read every real page first (index, titles, headings, bodies) so every query and `relevant` path is grounded in content that actually exists; write a header stating the file is AI-drafted and unreviewed; make each query probe a distinct hypothesis and annotate it with a `# đo:` comment naming the channel/case it tests (rare hyphenated term, mid-page bullet → chunk channel, pure paraphrase → vector, multi-relevance → P@k penalty, near-duplicate pages → dedup/ordering, shortest page in corpus, easy sanity check); flag the weakest items explicitly instead of padding the set; and end with a "Việc của bạn (human)" block listing exactly what the human must replace (queries → real questions from session history / `wiki/log.md`) plus the decision rule for reading the numbers (only flip a feature when Δ on THIS set is clearly positive; ±0.02 at this scale is noise). Numbers from an AI-judged set may be used to detect ordering regressions but must be reported as insufficient to conclude "better for you". Confidence: 0.7